#include <stdio.h>
#include <string.h>

int main()
{
    int n, k, i, t;

    scanf("%d", &t);

    while(t--)
    {
        scanf("%d %d", &n, &k);

        for (i = 1; i <= k; i++) 
        {
            if 
            printf("%d ", i);
        }

        printf("\n");
    }

    return 0;
}